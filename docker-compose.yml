services:
  bot:
    build: .
    image: ghcr.io/infuseting/betterticket:latest
    container_name: betterticket-bot
    restart: always
    env_file:
      - .env
    volumes:
      - ./prisma:/app/prisma
